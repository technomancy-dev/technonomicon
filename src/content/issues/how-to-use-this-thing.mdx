---
title: This is a Digital Garden, so you will need some gardening tools.
description: Robots are fucking us2.
issue_number: 2
layout: "../../layouts/Issue.astro"
---

import { getEntry } from "astro:content";
import ArticleTeaser from "../../components/ArticleTeaser.astro";
import FancyParagraph from "../../components/Content/FancyParagraph.astro";
import Empty from "../../components/Empty.astro";
import TitleLink from "../../components/TitleLink.astro";

export const components = {
  h1: props => <h1 class="col-span-12 text-6xl text-left" {...props} />
}

export const what_are_issues = await getEntry(
    "articles",
    "what-are-issues",
);

export const what_are_articles = await getEntry(
    "articles",
    "what-are-articles",
);

# {frontmatter.title}

<ArticleTeaser
  markdown={what_are_issues}
  components={{ h1: props => <TitleLink slug={what_are_issues.slug} {...props} />}}
  class="lg:col-span-6 prose-xl col-span-12"
  cta="Read more about Issues"
/>

<ArticleTeaser
  markdown={what_are_articles}
  components={{ h1: props => <TitleLink slug={what_are_articles.slug} {...props} />}}
  class="lg:col-span-6 prose-xl col-span-12"
  cta="Read more about Articles"
/>
