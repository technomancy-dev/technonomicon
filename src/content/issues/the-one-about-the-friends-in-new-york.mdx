---
title: Could I be anymore played out of a reference?
description: Robots are fucking us.

issue_number: 3
---

import { getEntry } from "astro:content";
import SnippetLink from "../../components/SnippetLink.astro";
export const technonomicon = await getEntry("articles", "technonomicon");

{console.log(technonomicon)}

<div class="grid grid-cols-12 gap-16 px-10 text-justify">
    <SnippetLink href={`/articles/${technonomicon.slug}`} class="col-span-4 row-span-2" title={technonomicon.data.title}>
        {technonomicon.body}
    </SnippetLink>
    <h1 class="text-6xl col-span-8 text-left">{frontmatter.title}</h1>
    <SnippetLink class="col-span-8" title={technonomicon.data.title}>
        {technonomicon.body}
    </SnippetLink>
    <SnippetLink class="col-span-8" title={technonomicon.data.title}>
        {technonomicon.body}
    </SnippetLink>
        <SnippetLink class="col-span-4 row-span-2" title={technonomicon.data.title}>
        {technonomicon.body}
    </SnippetLink>
    <h2 class="text-6xl col-span-8 text-left">{frontmatter.title}</h2>
</div>
