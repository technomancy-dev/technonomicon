---
const { sup, sub, number, frequency } = Astro.props;
---

<div
  class="font-display w-1/2 bg-gradient-to-r from-base-content via-white bg-300% to-base-content text-transparent bg-clip-text animate-gradient"
>
  <p class="text-xs">{sup}</p>
  <div class="flex items-baseline">
    <span class="text-4xl inline">{number}</span>
    <span class="text-xs inline">/{frequency}</span>
  </div>
  <p class="text-xs">{sub}</p>
</div>
